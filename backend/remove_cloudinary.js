const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const images = await prisma.productImage.findMany(); for (const img of images) { if (img.url && img.url.includes('res.cloudinary.com')) { const newUrl = '/products/' + img.url.split('/').pop(); await prisma.productImage.update({ where: { id: img.id }, data: { url: newUrl } }); } } console.log('Removed Cloudinary from DB completely.'); } main().catch(console.error).finally(()=>prisma.$disconnect());
